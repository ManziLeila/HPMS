# API Base URL (optional, for local dev only)
# - Production: leave unset. The app uses the same domain (e.g. https://payroll.hcsolutions-rw.com/api).
# - Local: leave unset to use http://localhost:4000/api, or set if your backend runs on another port:
# VITE_API_BASE_URL=http://localhost:4000/api
